### Day 5: Learned Git Branching

Today I learned that branches allow developers to safely experiment without affecting the main project. I created my first feature branch, made changes, merged the branch back into main, and learned how Git tracks commits using pointers. I also learned how to recover when a commit accidentally lands on the wrong branch.